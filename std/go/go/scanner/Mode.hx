package go.go.scanner;

/**
    A Mode value is a set of flags (or 0).
    They control scanner behavior.
**/
@:go.Type({ name: "Mode", instanceName: "scanner.Mode", imports: ["go/scanner"] })
extern typedef Mode = go.GoUInt